This program counts the number of occurences of words in folder structure. It runs in parallel. 

To run this, import it in eclipse java project and run Main method in class WordCounter.java OR you can use command javac to compile and command java to run this program.